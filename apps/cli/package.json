{
  "name": "@agent-ui/cli",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/cli.ts",
    "eval:search": "bun run src/eval-search.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  }
}

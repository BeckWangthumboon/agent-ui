{
  "name": "@agent-ui/cli",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/cli.ts",
    "eval:search": "bun run src/evalSearch.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  }
}
